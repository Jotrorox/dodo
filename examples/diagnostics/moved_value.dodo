package diagnostics

struct Value {
    number: i32
}
fn consume(value: Value) {}

// Intentionally rejected: Value is moved by the first call.
fn main() {
    value := Value { number: 1 }
    consume(value)
    consume(value)
}
