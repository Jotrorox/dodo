package diagnostics

fn consume(view: &i32) -> i32 {
    return *view
}

// Intentionally rejected: the later use keeps the shared borrow live.
fn main() -> i32 {
    value := 1i32
    view := &value
    value = 2
    return consume(view)
}
