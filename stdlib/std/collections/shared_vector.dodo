package shared_vector

import "alloc/shared_arena"
import "std/collections/vector"

pub fn new<T>(allocator: shared_arena.Handle) -> vector.Vector<T, shared_arena.Handle> from(allocator) {
    unsafe {
        return vector.new::<T, shared_arena.Handle>(allocator)
    }
}
