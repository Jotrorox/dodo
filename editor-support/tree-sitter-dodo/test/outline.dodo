package outline

struct Buffer<T> {
//     ^ name
    value: T
    fn view(&self) -> &T from(self) {
//     ^ name
        return &self.value
    }
}

enum State { Ready, Failed(code: i32) }
//   ^ name

extern fn foreign(value: i32) -> i32
//        ^ name

fn main() -> i32 {
// ^ name
    return 0
}
